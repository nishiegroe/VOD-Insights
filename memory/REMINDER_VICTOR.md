# Reminders for Nishie

** Today at 6pm CST **

## 1. Unblock Victor
```bash
sudo apt-get install build-essential libvlc-dev libvlccore-dev
```

## 2. Install Gmail Python libs (for email summarizer skill)
```bash
pip install google-auth google-auth-oauthlib google-api-python-client
# OR if using venv:
source ~/.venv/bin/activate && pip install google-auth google-auth-oauthlib google-api-python-client
```

---
Set by Larry 2026-03-02
